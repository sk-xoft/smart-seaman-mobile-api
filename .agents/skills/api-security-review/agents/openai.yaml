interface:
  display_name: "API Security Review"
  short_description: "Review this API for authentication and data security risks"
  default_prompt: "Use $api-security-review to review a change for JWT authorization, SQL injection, upload, secret, logging, CORS, and storage risks."
